// Define object statucEnums to be used as reference code in application
const staticEnums = {
	// Values for Users roles
	ROLES: {
		USER: 1,
		ADMIN: 2,
	},
	// Values for filtering users by isActive
	ACTIVE_FILTER: {
		ACTIVE: true,
		INACTIVE: false,
	},
};

// Export staticEnums
module.exports = staticEnums;
